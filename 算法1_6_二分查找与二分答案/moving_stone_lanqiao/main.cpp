// https://lx.lanqiao.cn/problem.page?gpid=T2400
#include <iostream>
using namespace std;
int n,m;int stones[1000010];
bool check(long long int mid) {
    if
}

int main()
{
    ios::sync_with_stdio(false);
    cin.tie(0);cout.tie(0);
    cin>>n>>m;
    for(int i=0;i<n;i++) {
        cin>>stones[i];
    }
    sort(begin(stones),begin(stones)+n);

    long long int l=0,r=1000000,ans;
    while (l<=r) {

    }
    return 0;
}
